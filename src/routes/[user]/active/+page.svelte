<script>
// @ts-nocheck

    /**
     * 
     * NEXT BIG TODO!
     * 
     * Need to make sure that the files are uploaded to a folder with our respective names.
     * 
     * 
     */
    export let data;
    const user = data.user;

</script>

<article
    id={`${user}-images`}
    class="h-full w-full flex flex-row gap-20 align-center justify-center items-center" 
>
    <img alt="current-user-quiet" src=""/>
    <img alt="current-user-speak" src=""/>
</article>